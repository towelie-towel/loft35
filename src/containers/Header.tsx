import Nav from "../components/Nav";
import HeaderView from "../components/Header_View";

const Header = () => {
  return (
    <header>
      <Nav />
      <HeaderView />
    </header>
  );
};

export default Header;
